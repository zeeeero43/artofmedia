# Art.of.Media Backend - Production Environment Variables
# Copy this file to .env on your VPS server

# Node Environment
NODE_ENV=production

# Server Configuration
PORT=3001
HOST=127.0.0.1

# SMTP Configuration (for contact form)
# Use your preferred email service (Gmail, SendGrid, Mailgun, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password

# Email Addresses
EMAIL_FROM=contact@artofmedia-marketing.de
EMAIL_TO=admin@artofmedia-marketing.de

# CORS Configuration
CORS_ORIGIN=https://artofmedia-marketing.de,https://www.artofmedia-marketing.de

# Security
# Generate a random secret: openssl rand -base64 32
SESSION_SECRET=your-random-secret-key-here

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX_REQUESTS=100  # max 100 requests per window

# Optional: Analytics
# GOOGLE_ANALYTICS_ID=UA-XXXXXXXXX-X

# Optional: Sentry Error Tracking
# SENTRY_DSN=https://xxx@sentry.io/xxx
